<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" href="/styles/main.css" />
</head>
<body>
    <title>Edit Your Road Below</title>
    <a href="/">Back to Cars and Roads</a>

    <div>
        <form action ="/location/<%= locations._id %>?_method=PUT" method="post" id="createForm">
            <input type="text" name="road" placeholder="Update road name" required value="<%= locations.road %>" />
            <input type="text" name="location" placeholder="Update road location" required value="<%= locations.location %>" />
            <input type="text" name="description" placeholder="description of road" value="<%= locations.description %>" />
            <input type="text" name="difficulty" placeholder="difficulty level" value="<%= locations.difficulty %>" />
            <input type="text" name="visited" placeholder="Visited" value="<%= locations.visited%>" />
            <input type="text" name="img" value="<%= locations.img %> " />
            <button type="submit">Submit Edit</button>
        </form>
    </div>
</body>
</html>
